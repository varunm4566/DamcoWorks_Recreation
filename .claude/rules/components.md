---
paths:
  - "src/components/**"
  - "src/pages/**"
---
# Component Rules
- Functional components only — no class components
- Props must be destructured at the top of the function
- Loading and error states must be handled for all data fetches
- All clickable elements must have accessible aria-labels
